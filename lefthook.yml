pre-commit:
  commands:
    biome:
      root: "web-service/"
      run: |
        npm run lint
        npx tsc --noEmit

pre-push:
  commands:
    biome:
      root: "web-service/"
      run: |
        npm run lint
        npx tsc --noEmit