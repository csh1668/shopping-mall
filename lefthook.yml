pre-commit:
  commands:
    biome:
      root: "web-service/"
      run: npm run lint
      stage_fixed: true
      

pre-push:
  commands:
    biome:
      root: "web-service/"
      run: npm run lint